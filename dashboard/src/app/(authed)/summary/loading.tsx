/**
 * Loading skeleton — inherits the shape used across the report-style
 * pages. See sessions/loading.tsx for the annotated variant; this
 * file is identical so nav feedback is consistent.
 */
export { default } from "../sessions/loading";
